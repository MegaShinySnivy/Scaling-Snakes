---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/helm.toolkit.fluxcd.io/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: redbot-test
  namespace: redbot-test
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controller:
      annotations:
        reloader.stakater.com/auto: "true"
    image:
      repository: "jank.ing/jjgadgets/redbot"
      tag: "3.5.5@sha256:f2aa73d9de7cb466a37fb92dfd9aa81d8a08db199105bf196f1363df09288102"
    env:
      TZ: America/Chicago
      NAME: Pineapple
      PREFIX: "pl."
      STORAGE_TYPE: JSON
      DATA_PATH: /data
      CONFIG_PATH: /config
    envFrom:
      - secretRef:
          name: redbot-test-secret
    service:
      main:
        enabled: false
    ingress:
      main:
        enabled: false
    persistence:
      config:
        enabled: true
        existingClaim: redbot-test-config
        retain: true
      data:
        enabled: true
        existingClaim: redbot-test-data
        retain: true
      requests:
        cpu: 10m
        memory: 250Mi
      limits:
        memory: 2000Mi
