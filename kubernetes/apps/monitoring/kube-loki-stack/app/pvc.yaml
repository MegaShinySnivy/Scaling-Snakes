---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-loki-stack
  namespace: default
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: csi-ceph-blockpool
  resources:
    requests:
      storage: 30Gi
